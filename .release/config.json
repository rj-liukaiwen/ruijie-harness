{
  "schemaVersion": 1,
  "name": "Ruijie-Harness",
  "node": "24.12.0",
  "packageManager": "yarn",
  "versionFiles": [
    "package.json",
    "dsh-plugin-desktop/package.json"
  ],
  "adapter": ".release/adapter.mjs",
  "targets": [
    "linux-x64"
  ],
  "signing": "testing",
  "allowPrerelease": false,
  "acceptanceBaseline": "v2.0.7",
  "releaseNotes": [
    "Candidate only: real SSO/model/IM, upgrade, and physical macOS TCC acceptance remain required.",
    "The historical v2.0.7 tag is used to include the complete visible change range; it is not claimed to have passed human acceptance.",
    "Linux is a new experimental packaging target. Its installed runtime and desktop integration require validation.",
    "GitHub Release does not update the existing gptauth.ruijie.com.cn application update service."
  ]
}
